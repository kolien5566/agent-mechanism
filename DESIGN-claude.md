# AI Agent 课件视觉规范

课件面向非技术听众讲解通用 Agent 原理与基本使用。原生页面以信息清楚、投影可读为目标，采用暖纸色与赤陶色。当前 14 张机制配图恢复原版，保留原图的构图与视觉表现。

## 画布与颜色

- 画布：16:9，1280 × 720；左右 64px、顶部 44px，底部预留页脚。
- 正文背景：`#F5F4ED`；内容分区：`#FAF9F5`；主文字：`#141413`；正文：`#383833`；辅助文字：`#62625A`。
- 强调色：`#C96442`；浅强调底：`#F0E4DC`；分隔线：`#DEDED4`。
- 深色 `#242521` 用于封面与章节过渡页；原生正文页面统一浅底。以上颜色规范用于页面排版，原版配图保持现状。

## 字体与密度

统一中文无衬线字体：`PingFang SC, Microsoft YaHei, Noto Sans CJK SC, system-ui, sans-serif`。

- 封面：54px；章节标题：54px；正文页标题：40px；分区标题：28px。
- 正文：26px；表格/紧凑流程：24px；辅助说明：22px；页码等非核心信息：18px。
- 标题行高约 1.22，正文约 1.45。普通页标题优先一行，最多两行。
- 一页一个概念，通常 3—4 个要点。解释页约 120—180 个中文字，多余细节放在 HTML 讲者备注。
- 使用简短标题、短段落和真实对照，不通过 `zoom` 或局部小字号挤入过量内容。

## 版式

- 定义：一句定义 + 组成或作用。
- 机制：原生标题 + 主结构图，完整展示原版图片。
- 对照：左右分栏、三项短对照或表格。
- 流程：三至五个节点，箭头有清楚的意义；非顺序关系不画成强制流程。
- 案例：任务输入、执行条件、结果与检查。
- 演示：一张可辨认的大截图，配最多三项提示。

卡片仅用于分组；同页不叠加多层卡片、眉题、编号和口号。产品概览用表格，避免五张缩略截图。页脚仅展示章节与页码；封面与章节页隐藏页脚。

## 图片

正式图片保存在 `public/generated-images/`。当前课件使用 14 张原版 `.png`，对应 `*-v2.png` 文件保留供追溯，未被课件引用。具体清单见 `public/generated-images/image-plan.md`。

- 图片恢复保留 **62 页**、已修改的文字、讲者备注和章节安排。
- 原版图片沿用其原有标题、标签、构图和风格；页面标题仍由 Slidev 呈现。
- 机制图和截图默认 `object-fit: contain`，完整保留图内内容。实际截图可围绕讲解目标选择区域。
- 课件引用项目内图片，不依赖个人生成目录。后续图片更换同时更新资产索引与页面引用。

## 文风与检查

页面平铺直叙地解释概念、条件和结果；移除“学生版理解”“这一页要记住”等教师视角标签。讲解页不放作者归属、来源脚注；资料链接收在备注与课后阅读。

修改后运行 `npm run build`，核对 62 页顺序、正文与图中术语，再逐页查看 1280 × 720 呈现。重点检查页面数量、图片加载、溢出、裁切、中文文字和页脚，确认恢复原图后仍完整可读。
